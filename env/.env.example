# Model Provider Configuration
# Set to "bedrock" to use AWS Bedrock, "openai" for OpenAI (default: openai)
MODEL_PROVIDER=bedrock

# OpenAI Configuration (if MODEL_PROVIDER=openai)
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL_ID=gpt-4o

# AWS Bedrock Configuration (if MODEL_PROVIDER=bedrock)
# Requires AWS credentials configured via `aws configure` or environment variables
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.anthropic.claude-haiku-4-5-20251001-v1:0
BEDROCK_MAX_TOKENS=8192

# Server Configuration
AGENT_PORT=8000
AGENT_PATH=/

# Frontend URL (for CORS if needed)
NEXTJS_API_URL=http://localhost:3000

# Strands Agent URL (set in Next.js .env.local)
# STRANDS_AGENT_URL=http://localhost:8000
