{
  "name": "copilotkit-strands",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix _internal run dev",
    "dev:ui": "npm --prefix _internal run dev:ui",
    "dev:agent": "npm --prefix _internal run dev:agent",
    "build": "npm --prefix _internal run build",
    "start": "npm --prefix _internal run start",
    "lint": "npm --prefix _internal run lint",
    "install:deps": "npm --prefix _internal install",
    "postinstall": "npm run install:deps"
  }
}
